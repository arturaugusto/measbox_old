<%= simple_form_for(@model) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :model_list, label: 'Model tags' %>  
    <%= f.input :name, input_html: { class: 'form-control' } %>
    
    <%= render "shared/modal_choose_value", f: f, :association => :manufacturer, :cols => [:name, :created_at] , :value => (@model.manufacturer.nil? ? "Choose a manufacturer..." : @model.manufacturer.name) %>
    
    <%= render "shared/modal_choose_value", f: f, :association => :kind, :cols => [:name, :created_at] , :value => (@model.kind.nil? ? "Choose a type of instrument..." : @model.kind.name) %>    

  </div>
  <br>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn btn-primary' %>
  </div>
<% end %>
